# Core dependencies (required)
edge-tts>=6.1.0
unidecode>=1.3.0

# Optional dependencies
pykakasi>=2.2.1  # Japanese text romanization (optional)
pycaw>=20240210; platform_system == "Windows"  # Windows audio control (Windows only)